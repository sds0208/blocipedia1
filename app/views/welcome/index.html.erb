<div class="center-block jumbotron">
    <h2>Blocipedia: Social, Markdown Wikis</h2>
</div>
<div class="center-block thumbnail">
    <h5>Create and share Markdown Wikis</h5>
    <%= link_to "Sign Up", new_user_registration_path %>
</div>  
<div class="container center-block">    
    <div class="row">
            <div class="col-sm-6">
                <h3>Create public wikis!</h3>
                <%= link_to "Free Plan", edit_user_registration_path, class: 'btn-lg btn-primary' %>
                <% if current_user && current_user.role == 'premium' %>
                    <h5>(Downgrade Membership)</h5>
                <% end %>    
            </div>
            <div class="col-sm-6">
                <h3>Create private wikis!</h3>
                <%= link_to "Premium Plan", new_charge_path, class: 'btn-lg btn-primary' %>
                <% if current_user && current_user.role == 'standard' %>
                    <h5>(Upgrade Membership)</h5>
                <% end %>    
            </div>
    </div>
</div>   
